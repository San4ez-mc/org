# Дорожня карта — Жива Орг.Платформа

> Живий бэклог. Статуси: ✅ готово · 🔄 в роботі · ⬜ заплановано · 💬 треба обговорити.
> Оновлюється по ходу. Останнє оновлення: 2026-07-16.

---

## ✅ Готово (фундамент)

- ✅ Платформа задеплоєна на VPS (`org.fineko.space`, https + SSL), pm2: org-api / org-worker / org-web.
- ✅ Бот `@Fineko_proccesses_bot` створює компанію наскрізь: діалог → мапінг посад на 7 відділень (Хаббард) + PAEI (Адізес) → Drive + БД.
- ✅ База знань (RAG, ~70 статей, Vertex embeddings) — бот обґрунтовує рішення.
- ✅ Голос (Whisper), Claude-агент, канонічні відділи (21) з ЦКП, керівники відділень/відділів.
- ✅ Веб-пульт: список компаній, орг-борд (7 відділень в лінію + відділи, зум), процеси, тонкий топбар + вузьке меню з розділами.
- ✅ Ембединги на Vertex (проект proccess-org-ai-bot).

---

## 🔄 В роботі

- 🔄 **Swimlane бізнес-процесів (Mermaid).** ШІ генерує діаграму з лейнами по відповідальних (вкл. Клієнт/Система/ШІ), рішення+гілки; пульт рендерить графічно. *(поточна робота)*

---

## ⬜ UI / Сторінки (найближче)

- ✅ **Вибір компанії** — випадайка у верхньому барі справа (перемикає компанію) + **«Вийти»**.
- ✅ **Сторінка «Працівники»** — список, додавання/видалення, призначення на посади, **редагування** (ім'я/прізвище/TG id/нікнейм/дата народження/пошта), **пошук-фільтр**, **пагінація**. ⬜ фото з Telegram (getUserProfilePhotos).
- ✅ **Баг «багато керівників» виправлено** — прибрано 189 випадкових призначень; seed не чіпає керівницькі посади (голова/керівник = один тримач).
- ✅ **Процеси — редактор:** ✅ структурний (кроки: відповідальний/дія/результат, порядок ↑↓, авто-діаграма) + ✅ **canvas (React Flow)** — кнопка «◆ Схема»: додавати/видаляти/зʼєднувати ноди мишкою, гілки-рішення з мітками (так/ні), лейни-кольори за відповідальним, збереження у `graph`.
- ✅ **Вкладки прибрано** — навігація в лівому меню.
- ✅ **Inline-редагування назв/ЦКП** відділень і відділів (клік → інпут → збереження).
- ✅ **Показ людей на борді + перемикач.**
- ✅ **Сторінка «Інструкції»** — дерево папок з `Відділення побудови/Посадові інструкції` (згортання); клік → відкриває Doc на Drive.
- ⬜ **Фавікон.**
- ⬜ **Скроли** — стилізувати під темну тему (зараз системні).
- ⬜ **Легенда PAEI (Адізес)** зверху — які кольори що означають.
- ⬜ **Сторінка «Налаштування»** — підключити свій Drive, вибрати папку(и), інструкції як це зробити + інші налаштування.
- ⬜ **Сторінка «Журнал змін»** (див. розділ нижче).
- ⬜ **Сторінка «Логи»** (технічні: помилки/запуски ботів — для дебагу проблем користувачів).
- ⬜ **Чат з ботом** — іконка справа знизу (як у контент-платформі) + копія воронки бота-агента. *(пізніше, коли бота донавчимо)*

---

## ⬜ Орг.структура (глибше)

- ⬜ **Посади всередині відділів** (а не внизу відділення). ЦКП по компанії в цілому + по відділах (з таблиці) — перенести.
- ⬜ **Працівники всередині посад** + **перемикач «показувати людей»** зверху. Кілька людей на одній посаді.
- 🔄 **Редагування:** ✅ додати/видалити працівників, ✅ додати/видалити посади (у борді + панелі), ✅ inline-редагування назв/ЦКП. ⬜ лишилось: drag&drop перенесення посад між відділами; попап для складніших форм (статуси, долі).
- ⬜ **Інвестори та кілька співвласників з долями (%).** 💬 *Пропозиція: окремий блок «Власники/Інвестори» над бордом; модель `owners` (member, роль=owner/investor, sharePct). Бот питає про власників на онбордингу.*
- ⬜ **Кілька підрозділів** (напр. виробництво + роздрібна точка). 💬 *Пропозиція: материнська компанія + дочірні орг-структури (parent_company_id). Бот на онбордингу визначає: одна структура (функції) чи кілька (окремі бізнес-юніти з власними відділеннями) + материнські адмін/фінанси.*
- ⬜ **Експорт** орг.структури як зображення (PNG/SVG) + PDF. 💬 *Пропозиція: PNG/SVG (html-to-image / Mermaid export), PDF — друк сторінки.*

---

## ⬜ Бізнес-процеси (глибше)

- 🔄 Детальний **swimlane** (Mermaid, лейни=відповідальні). *(в роботі)*
- ✅ **Коментування кожного блоку** процесу (поле «Коментар до блоку» в редакторі, 💬 у перегляді).
- ✅ **Позначка «проблемний блок»** — червоний бейдж + підсвітка рядка.
- ✅ **Позначка «можна автоматизувати»** + апсел-текст «зверніться до Олександра» (mailto) — заощадите час/гроші.
- ⬜ **Зв'язок з орг.структурою:** крок → посада за `id` (не за назвою); на сторінці посади — «процеси, в яких бере участь».
- ⬜ **Показник опису процесів:** скільки описано + % деталізації кожного (напр. 30/100%).
- ⬜ **Експорт** кожного процесу як зображення (+ інші формати).
- ✅ **Редактор процесів** — структурний + canvas (React Flow), див. блок «UI/Сторінки».

---

## 💬 Редагування структури/процесів + версійність (велике, обговорюємо)

Проблема: є **поточна** структура/процеси і **планована/тестова** (відділи яких ще нема, застарілі процеси, ті що тестуються; чинні керівники vs заплановані).

💬 **Пропозиція (для обговорення):**
- Кожна одиниця (відділення/відділ/посада/процес) має **статус:** `current` (діюча), `planned` (планована), `testing` (тестується), `deprecated` (застаріла).
- **Перемикач зверху:** «Тільки поточне» / «Показати все (з планованим)».
- **Візуальне розрізнення** статусів — рамкою/штрихуванням/іконкою (⚠ НЕ кольором фону, бо колір = PAEI Адізеса). Напр.: пунктирна рамка = planned, жовтий бейдж «тест» = testing, приглушено = deprecated.
- **Історія версій** одиниці (хто, коли, що змінив).
- **Інструменти редагування доступні боту** (через API): бот може створювати тестові відділи/процеси, міняти статуси.

---

## 💬 Журнал змін + система затверджень (велике)

- ✅ **Сторінка «Журнал змін»** (5 колонок: Орг.структура · Процеси · Інструкції · Автор · Затверджено). Зміни логуються автоматично (ChangeLog) на мутаціях. ⬜ лишилось: система затверджень (нижче).
- ✅ **Сторінка «Логи»** (технічні події ботів/api — EventLog).
- Кожна зміна торкається 3 сутностей (структура/процеси/інструкції) — журнал показує зв'язані зміни разом.
- 💬 **Система затверджень (обговорюємо):** хтось пропонує зміну (`proposals`), хтось затверджує. Варіанти: (а) прямий керівник; (б) усі вищестоящі керівники по ланцюгу; (в) власник. Ймовірно — конфігуровано per-компанія. Зв'язати з розповсюдженням змін («живий організм»).

---

## ⬜ Доступ / Ролі

- ✅ **Вхід за паролем** — сесійний cookie (30 днів), middleware захищає всі сторінки, `/login`, кнопка «Вийти». Пароль/токен у `apps/web/.env.local` на сервері.
- ✅ **Супер-адмін** — єдиний адмін-вхід бачить УСІ компанії (перемикач у топбарі).
- ⬜ Багатокористувацькі ролі per-компанія: owner / head / employee (enum MemberRole є) — коли з'явиться вхід для клієнтів (кожен бачить свою компанію/свій рівень).

---

## ⬜ Живий організм (ядро — розповсюдження змін)

- ⬜ Зміна інструкції → вектор-пошук пов'язаних → ШІ пропонує правки → затвердження → зміни розходяться. *(вектори готові, лишається worker-джоб + proposals + inline-кнопки)*

---

## ⬜ Майбутнє (ШІ-агенти)

1. ⬜ Правила генерації посадових інструкцій (прописати).
2. ⬜ **Генерація тексту вакансії** — коли в структурі позначено «треба найняти», агент генерує оголошення.
3. ⬜ **Автоматизація онбордингу працівника** — маємо інструкції по посаді → навчання + тестування знань.
4. ⬜ **Етап компанії за Адізесом** — визначити стадію → побудувати цільову структуру для переходу на наступний рівень (тестові відділи, агент їх створює).
5. ⬜ **Аналіз структури/процесів агентами** — виявлення слабких місць, рекомендації за Адізесом що змінити (механізм тестових відділів/процесів тут стане в пригоді).

---

## ⬜ Прогалини з консультантського аудиту (погляд 4 ролей)

> Додано після аудиту «очима» консультанта / власника / HR / працівника. ⭐ = критично для повноцінної роботи.

### 👑 Власник / керування
- ✅ **Статистики по ЦКП** — сторінка «📈 Статистики»: у одиниці (відділення/відділ/посада) вимірюваний показник + SVG-графік тренду, останнє значення зі стрілкою ↑/↓ і кольором (краще вгору/вниз), додавання/видалення точок по датах. Модель `Statistic` (points JSON), API CRUD + `/points`. ⬜ далі: ввід чисел через бота; sparkline прямо на орг-борді; дашборд усіх трендів на головній.
- ⭐ **Дашборд власника** — здоров'я компанії з одного погляду: заповнено/вакантно, % опису процесів, зміни за період, що чекає затвердження, вузькі місця.
- **Стратегічний шар** — мета компанії, її власний ЦКП, ідеальна картина, цілі; структура/процеси служать цьому «навіщо».
- **Вартість оргструктури** — ФОП по відділеннях, планування штату.

### 👨‍💼 Консультант (впровадження)
- ⭐ **Імпорт існуючих даних** — масовий залив людей/посад/відділів з Excel/CSV/Google Sheet (не лише бот з нуля).
- ⭐ **Діагностика «як є» (org-health)** — екран-аудит: посади без ЦКП, % описаних процесів, вакансії, посади без інструкції, люди без посади. Головний продукт консультанта.
- ✅ **Бібліотека шаблонів** — сторінка «📚 Шаблони» (`/company/[id]/templates`): 3 галузеві шаблони (кафе/ресторан, роздрібний магазин, IT-послуги), кожен — типові посади (додаються у відповідне канонічне відділення), процеси (з кроками) й інструкції (`Instruction`-рядки, статус DRAFT). Статичний каталог `packages/org-template/src/library.ts` (як `CANONICAL_DIVISIONS`/`plans.ts` — галузі змінюються рідко). `POST /companies/:id/templates/:key/clone` з чекбоксами що клонувати; ідемпотентно — пропускає те, що вже є за назвою, тож повторний клік не плодить дублікатів. Побічний ефект: `Instruction`-таблиця нарешті отримала реального продюсера (раніше жоден код її не наповнював). ⬜ далі: більше галузей, попередній перегляд кроків процесу (не лише назв), клонування структури глибше за один POST-рівень.
- **Портфель клієнтів** — супер-адмін веде багато компаній: порівняння, клонування налаштувань, статуси впровадження.

### 🧑‍💼 HR
- ⭐ **Картка працівника й життєвий цикл** — дата прийому, історія посад (переміщення/підвищення), випробувальний, документи/договори, статус (працює/звільнений).
- ⭐ **Облік навчання й тестування** — хто які інструкції прочитав і склав тест, результати. *(доповнює онбординг-автоматизацію з блоку ШІ)*
- **Рекрутинг-воронка** — вакансія → кандидати → найм → онбординг. *(генерація тексту вакансії вже в плані ШІ)*
- **Кадрові накази/політики** — розпорядження компанії окремо від посадових інструкцій.

### 👷 Працівник
- ✅ **Особистий доступ працівника** — вхід за особистим посиланням `/me/[token]` (без пароля, кнопка «🔗 Вхід» у панелі людей), персональна сторінка «Моя робота»: посади + ЦКП + шлях у структурі, мої показники (тренд), мої процеси (з підсвіткою моїх кроків). Без адмін-хрому. ⬜ далі: посадові інструкції (Drive) на сторінці, «прийняти інструкцію».
- 🔄 **Самообслуговування через Telegram-бот** — ✅ API-фундамент `GET /members/by-telegram/:tgId` (те саме зведення). ⬜ лишилось: гілка у флоу бота-агента (розпізнати користувача → відповісти «яка моя інструкція / як робиться процес X»).
- ⭐ **Ознайомлення/підпис** — «прочитав і приймаю інструкцію» (дата) → замикає відповідальність.
- **Запит на зміну від працівника** — пропозиція правки до своєї інструкції (годує proposals / «живий організм»).

### 🔩 Наскрізне
- ⭐ **Сповіщення** (Telegram/email) — затвердження, зміни, вакансії. Без них система затверджень і «живий організм» неактивні.
- **Глобальний пошук** — по людях/посадах/процесах/інструкціях.
- **Володіння даними** — повний експорт компанії (клієнт може забрати своє).

---

## ⬜ Комерція (Ф3)

- ⬜ Місячна підписка / білінг (слоти в схемі є).

---

## Мої додаткові пропозиції

- **Формати експорту:** орг.структура → SVG (векторний, масштабується) + PNG; процеси (Mermaid) → SVG/PNG нативно; звіт по компанії → PDF.
- **Редагування:** гібрид — inline для назв/ЦКП (клік→інпут), попап-форма для складних (додати посаду, призначити людину, змінити статус, долі власників).
- **Показник деталізації процесу:** рахувати по заповненості (кроки, відповідальні, коментарі, позначки автоматизації) → %.
- **Технічні логи:** окрема таблиця `event_log` (level, source=bot/api/worker, message, companyId, ts) + сторінка з фільтрами.
