# ЧЕКПОЙНТ: ORG «Папка / структура» — де зупинились (2026-07-30)

> Читати цей файл при поверненні до ORG. Стан фічі «індексація → пошук → токени → структура».

## ✅ Зроблено (задеплоєно на прод org.fineko.space)
Сторінка **📁 Папка** (`apps/web/app/company/[id]/folder/page.tsx`) — повний стек:
1. Підключення папки + дерево завжди видиме (`DriveTreeManager`) + виключення 🚫 (`driveExcludedIds`).
2. **Асинхронна індексація** у вектор (`DriveIndexPanel`, `services/driveIndexer.ts`) — прогрес, поважає виключення, пише `folder_id` кожного чанка.
3. **3c** «Що система зрозуміла» (`DriveDetectPanel`, `POST /companies/:id/detect-facts`) — Vertex-витяг відділів/посад/інструкцій/фактів.
4. **3d** Папка для інструкцій (`InstructionsFolderPanel`) — кандидати з дерева + **центральна** `1. Відділення побудови → Посадові інструкції → 7 департаментів` (`POST .../instructions-folder/central`). Поле `Company.driveInstructionsFolderId`.
5. **3e** Пропозиція структури (`StructureProposalPanel`, `POST .../propose-structure`, `PATCH .../structure-proposal`, `POST .../apply-structure`) — 2 панелі (наявне/пропозиція), редагування (назви/±/описи), **план змін кольорами** (keep/rename/move/new) + легенда + лічильники + попап ℹ з повним деревом. Apply поки лише СТВОРЮЄ теки (не переносить файли). Поле `Company.structureProposal` (Json).
6. **Пошук** (`DriveSearchPanel`, `POST .../search`) — RAG через флоус (`flows /api/rag/search` → vector + Vertex Gemini) + джерела.
7. **Токени** (`VectorTokensPanel`, `.../vector/tokens`) — folder-scoped під-токени (для воронок).

**Інфра:** кожна компанія — свій vector-проєкт (`Company.vectorProjectId/vectorToken`); embeddings **Vertex text-embedding-004 (EU europe-west4)**; RAG-відповідь **Vertex gemini-2.5-flash**.

## ⏭️ Незавершене (наступні кроки)
1. **3e переміщення файлів (варіант B)** — dry-run: ШІ мапить кожен файл → цільова тека (за `descSystem`), показати повний список «файл → тека», підтвердження, перенос пачками + **лог відкату**. Тестувати на 1-2 файлах. Можливо копіювання, не переміщення.
2. **Правила шаблону від власника** — оновити `STRUCTURE_TEMPLATE` (у `apps/api/src/routes/index.ts`) + `DIVISIONS_7` + `ШАБЛОН_структури_папок.md` під його бачення (чекаю).
3. **Ярлики інструкцій** — інструкції пишуться в центральну папку, у папки працівників — ярлики (при генерації інструкцій, крок 4).
4. **Крок 4** — AI-агент інтерв'ю → скелет орг-структури (трекер 1153).
5. **4 тест-компанії** для перевірки генерації: Онлайн-школа `209ce5ff`, Кав'ярня `3eb36771`, Діджитал `506e2c16`, Магазин `a6689cb4` (на ORG-Диску, тека «TEST-компанії FINEKO»).

## Ключі/ID
- FINEKO компанія `b07bc54b-5930-41ff-8bfb-45ab1cd5bde1`, vector-проєкт `d112389c`.
- flows RAG: `/api/rag/search` + `/api/rag/generate`, заголовок `X-Rag-Secret` (значення у `/root/.rag_secret` на VPS).
- Vertex-конектор у флоус `ba9ef333`; GCP-проєкт `proccess-org-ai-bot`, ключ `secrets/vertex-key.json` (vector + gitignored у org).
- ORG API токен для тестів (prod, з apps/web/.env `ORG_API_TOKEN`).
